# AI Platform Frontend (Telegram Mini App + Web)

Один `index.html` + `app.js` + `styles.css` без сборки.

## Что уже есть
- ✅ Полная интеграция с **Telegram WebApp API**: `ready()`, `expand()`, theme params, `MainButton`, `BackButton`, `HapticFeedback`, `CloudStorage`, `sendData`, popups.
- ✅ Рабочая навигация/роутинг внутри SPA (без перезагрузки).
- ✅ **Гостевой режим**: чат в DEMO + проекты локально.
- ✅ Прикрепление файлов в чат (drag&drop и 📎), превью, удаление.
- ✅ Раздел **Медиа**: редактор фото (canvas) + базовый видео trim (опционально через `ffmpeg.wasm`).
- ✅ Экспорт/импорт данных (чат/проекты/настройки/ключи) в JSON.
- ✅ Диагностика (`diagnostics.json`).

## Важно про бэкенд
Некоторые разделы требуют бэкенд:
- Keys / Reminders / Calendar / Notifications — сейчас это подготовленные UI-экраны.
- Реальная отправка вложений на сервер (multipart upload) тоже делается в бэкенде.

Для Telegram-авторизации ожидается эндпоинт:
- `POST /api/v1/auth/telegram` с `{ init_data: tg.initData }` → `{ access_token: "..." }`

Для LIVE-чата (если отключить DEMO) ожидается:
- `POST /api/v1/chat` → `{ text: "..." }` (или совместимый формат)

## Локальный запуск

### Вариант 1: просто открыть
Открой `index.html` в браузере.

### Вариант 2: Docker + Nginx (и proxy на backend)
```bash
docker compose up --build
```

## Деплой на GitHub Pages
На Pages нет nginx proxy, поэтому в **Настройки → API base** укажи полный URL:
`https://your-domain/api/v1`

---
UI version: v2
